#version 330 core

in vec2 TexCoord;

out vec4 FragColor;

uniform sampler2D texture1;
uniform sampler2D texture2;
uniform float randColor;
uniform float randColor2;
uniform float mixWeight;

void main()
{
    //FragColor = texture(texture1, TexCoord) * vec4(ourColor.x, randColor, randColor2, 1.0);
    FragColor = mix(texture(texture1, TexCoord), texture(texture2, vec2(1.0 - TexCoord.x, TexCoord.y)), .2f);
}
